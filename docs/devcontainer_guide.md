# Dev Container を使った開発環境構築ガイド

初めてプログラミングに触れる方でも迷わずセットアップできるよう、ひとつひとつ丁寧に説明します。

---

## Dev Container とは？

**Dev Container（開発コンテナ）** とは、プログラムを動かす「専用の箱」をパソコンの中に作る仕組みです。

### イメージで理解する

```
あなたのパソコン
┌────────────────────────────────────────────────┐
│                                                │
│  ┌─────────────────────────────────────────┐  │
│  │  Dev Container（専用の箱）              │  │
│  │                                         │  │
│  │  Python 3.12                            │  │
│  │  必要なライブラリ（NumPy 等）           │  │
│  │  VS Code の拡張機能                     │  │
│  │                                         │  │
│  │  /data  ←─────────────────────────────────────── 外付けHDD（計測データ）
│  └─────────────────────────────────────────┘  │
│                                                │
└────────────────────────────────────────────────┘
```

この「箱」の中には、このプロジェクト（pyMEA）を動かすために必要なものがすべて入っています。

---

## なぜ Dev Container を使うの？（メリット）

### 1. 「自分のパソコンでは動いたのに…」が起きない
Python のバージョンやライブラリのバージョンが人によって違うと、同じコードが動いたり動かなかったりします。Dev Container を使うと全員が **まったく同じ環境** で作業できます。

### 2. パソコンを汚さない
Python やライブラリは「箱」の中にインストールされます。自分のパソコンに直接インストールしないので、後で不要になったら箱ごと削除するだけです。

### 3. セットアップが一瞬で終わる
「どのライブラリをインストールするか」の設定がすでに用意されています。コマンド1つで環境が自動的に整います。

### 4. チームで同じ環境を共有できる
設定ファイルがリポジトリに含まれているため、チームメンバー全員が同じ開発環境を使えます。

---

## 事前準備（必要なソフトウェア）

以下の4つをインストールしてください。すべて無料です。

### ステップ 1：Git をインストールする

> **Git とは？** ファイルの変更履歴を管理するツールです。リポジトリ（プロジェクトのファイル一式）をダウンロードするために使います。

- **Windows**: [Git for Windows](https://gitforwindows.org/) からダウンロード → インストーラーを起動して「Next」を押し続ける
- **Mac**: ターミナルを開いて `git --version` を実行 → インストールを促すダイアログが表示されたら指示に従う

インストール確認（Mac はターミナル、Windows は Git Bash を開いて実行）:
```bash
git --version
# git version 2.x.x と表示されれば OK
```

---

### ステップ 2：Docker Desktop をインストールする

> **Docker とは？** 「専用の箱（コンテナ）」を作るためのソフトウェアです。Dev Container はこれを使って動きます。

1. [Docker Desktop 公式サイト](https://www.docker.com/products/docker-desktop/) にアクセス
2. お使いの OS（Windows / Mac）向けのインストーラーをダウンロード
3. インストーラーを起動してインストール
4. インストール後、Docker Desktop を起動する（タスクバーやメニューバーにクジラのアイコンが表示されれば起動中）

> **注意（Windows の場合）**: WSL 2 のインストールを求められたら画面の指示に従ってインストールしてください。

インストール確認:
```bash
docker --version
# Docker version 2x.x.x と表示されれば OK
```

---

### ステップ 3：Visual Studio Code（VS Code）をインストールする

> **VS Code とは？** 無料で使えるコードエディタ（プログラムを書くためのソフト）です。Dev Container との連携機能が充実しています。

1. [VS Code 公式サイト](https://code.visualstudio.com/) からダウンロード
2. インストーラーを起動してインストール

---

### ステップ 4：Dev Containers 拡張機能をインストールする

VS Code を開いて、以下の手順で拡張機能をインストールします。

1. VS Code を起動
2. 左側のアイコンから **拡張機能（四角が4つのアイコン）** をクリック
3. 検索ボックスに `Dev Containers` と入力
4. **「Dev Containers」**（Microsoft 製）を選んで「インストール」をクリック

---

## リポジトリのクローンから Dev Container を開くまで

### ステップ 1：リポジトリをクローンする

> **クローンとは？** GitHub 上にあるプロジェクトのファイル一式を、自分のパソコンにコピーすることです。

**Mac** はターミナル、**Windows** は **Git Bash** を開き、以下のコマンドを実行します。

> **Git Bash の開き方（Windows）**: スタートメニューで「Git Bash」と検索して起動してください。コマンドプロンプトや PowerShell とは異なるので注意してください。

```bash
# ~/Workspace フォルダを作成（すでにある場合はスキップされる）
mkdir -p ~/Workspace

# Workspace フォルダに移動
cd ~/Workspace

# リポジトリをクローン
git clone https://github.com/kkito0726/MEA_modules.git
```

実行後、`~/Workspace/MEA_modules` というフォルダが作成されます。

---

### ステップ 2：計測データの場所を登録する（初回のみ）

> **重要**: Dev Container を開く前に必ずこの手順を完了してください。計測データが保存されている外付けHDDのフォルダをコンテナに接続するための設定です。

---

#### Windows の場合

リポジトリのフォルダ内にある **`setup.bat`** をダブルクリックして実行します。

```
MEA_modules/
├── setup.bat   ← これをダブルクリック
├── pyMEA/
├── .devcontainer/
└── ...
```

起動すると以下のような画面が表示されます。計測データが保存されているフォルダのパスを入力して Enter を押してください。

```
========================================
  pyMEA セットアップ
========================================

計測データが保存されているフォルダのパスを入力してください。
例: E:\MEA_data

データフォルダのパス: E:\MEA_data  ← ここに入力

========================================
  設定完了！
========================================

データフォルダ: E:\MEA_data

コンテナ内からは /data でアクセスできます。
例: read_MEA("/data/sample.hed", ...)

VS Code を再起動してから Dev Container を開いてください。
```

「設定完了！」と表示されたら、**VS Code を再起動**してください。

> **ドライブレターが変わったとき**（`E:` が `D:` になるなど）は `setup.bat` を再実行するだけで設定を更新できます。

---

#### Mac の場合

ターミナルを開き、以下のコマンドを実行して外付けHDDのパスを設定します。

```bash
# ~/.zshrc に追記（1行で実行）
echo 'export MEA_DATA_PATH="/Volumes/MyHDD/MEA_data"' >> ~/.zshrc

# 設定を反映
source ~/.zshrc
```

`/Volumes/MyHDD/MEA_data` の部分はご自身のフォルダパスに変更してください（Finder でフォルダを開き、アドレスバーからコピーできます）。

設定後は **VS Code を再起動**してください。

---

コンテナが起動すると外付けHDDのフォルダが `/data` として見えるようになります。コード内でのパス指定例:

```python
from pyMEA import *

# コンテナ内の /data が外付けHDDのフォルダに対応している
mea = read_MEA("/data/sample.hed", start=0, end=5, electrode_distance=450)
```

---

### ステップ 3：VS Code でフォルダを開く

```bash
# クローンしたフォルダを VS Code で開く
code ~/Workspace/MEA_modules
```

または、VS Code のメニューから **「ファイル」→「フォルダーを開く」** で `~/Workspace/MEA_modules` フォルダを選択します。

---

### ステップ 4：Dev Container で再度開く

VS Code でフォルダを開くと、右下に以下のような通知が表示されます。

```
┌─────────────────────────────────────────────────┐
│ フォルダーにデブコンテナー構成ファイルが含まれています。 │
│                                                   │
│  [コンテナーで再度開く]      [後で]               │
└─────────────────────────────────────────────────┘
```

**「コンテナーで再度開く」** をクリックしてください。

> 通知が表示されない場合は、VS Code 左下の **緑色のアイコン（`><`）** をクリック → 「コンテナーで再度開く」を選択します。

---

### ステップ 5：環境の自動セットアップを待つ

初回は Docker イメージのダウンロードとライブラリのインストールが自動で行われます。

```
[初回のみ] 数分かかる場合があります
  ↓
Python 3.12 の環境を準備中...
  ↓
必要なライブラリをインストール中 (NumPy, SciPy, Matplotlib 等)...
  ↓
VS Code の拡張機能をインストール中...
  ↓
完了！
```

VS Code の左下が以下のように変われば成功です:

```
Dev Container: Python 3
```

---

## 動作確認

Dev Container が起動したら、VS Code の **ターミナル**（メニュー：「ターミナル」→「新しいターミナル」）を開いて以下を実行してみましょう。

```bash
python --version
# Python 3.12.x と表示されれば OK
```

```bash
python -c "import pyMEA; print('pyMEA が使えます！')"
# pyMEA が使えます！ と表示されれば OK
```

---

## よくあるトラブル

### Q. 「コンテナーで再度開く」の通知が表示されない
**A.** VS Code 左下の緑色の `><` アイコンをクリックして「コンテナーで再度開く」を選択してください。

### Q. Docker Desktop が起動していないと言われる
**A.** Docker Desktop アプリを起動してから（タスクバー / メニューバーにクジラアイコンが表示されるまで待つ）、再度試してください。

### Q. 「WSL 2 インストールが必要」と表示される（Windows）
**A.** 表示されたリンクから WSL 2 をインストールしてください。インストール後にパソコンの再起動が必要な場合があります。

### Q. コンテナのビルドが途中で止まる
**A.** インターネット接続を確認してください。ライブラリのダウンロード中はネット接続が必要です。

### Q. コンテナ起動時にエラーが出てデータが見つからない（Windows）
**A.** `setup.bat` を実行する前に Dev Container を開こうとしている可能性があります。一度 Dev Container を閉じて `setup.bat` を実行し、VS Code を再起動してから再度開いてください。

### Q. `/data` の中身が空に見える
**A.** 外付けHDDがパソコンに接続されているか確認してください。接続されている場合は、`setup.bat`（Windows）または `~/.zshrc`（Mac）に設定したパスが正しいか確認してください。

### Q. OneDrive や Google Drive などのクラウドストレージに計測データを保存している
**A.** クラウドストレージの種類と設定によって動作が異なります。

| ストレージ | 動作 | 対処 |
|-----------|------|------|
| OneDrive / Dropbox（同期済み） | ○ | データフォルダを右クリック →「常にこのデバイスに保存する」に設定 |
| Google Drive（ミラーモード） | △ | 動く場合もあるが不安定 |
| Google Drive（ストリームモード・デフォルト） | ✗ | 仮想ファイルシステムのため Docker からアクセス不可 |

**OneDrive / Dropbox の注意点**: 「オンデマンドファイル（クラウドのみ）」の状態ではファイルの実体がローカルに存在しないため Docker から読めません。必ずローカルに保存されている状態にしてください。

**Google Drive の注意点**: デフォルトのストリームモードは仮想ファイルシステム（FUSE）を使用しており、Docker の bind mount と互換性がなく起動エラーになります。Google Drive の設定で「ミラーリング」モードに変更すると改善する場合があります。

**推奨**: MEA 計測データは1ファイルで数百MB〜数GBになることもあり、クラウドストレージ経由では読み込みが非常に遅くなります。**外付けHDDへの保存を推奨します。** クラウドストレージはバックアップ用途にとどめてください。

---

## アップデート方法

開発者から「アップデートしてください」と連絡が来たときは、以下の手順で最新版に更新します。

### ステップ 1：最新のコードを取得する（git pull）

VS Code のターミナルを開いて以下を実行します。

```bash
git pull
```

```
Already up to date.  ← すでに最新の場合
```
または
```
Updating abc1234..def5678  ← 更新があった場合
```

---

### ステップ 2：コンテナを再ビルドする

コードが更新されたらコンテナも作り直します。

VS Code 左下の **`Dev Container: Python 3`** をクリックし、**「コンテナーのリビルド」** を選択します。

```
┌────────────────────────────────────┐
│ コンテナーのリビルド               │  ← これを選ぶ
│ コンテナーで再度開く               │
│ ...                                │
└────────────────────────────────────┘
```

> リビルド中は初回起動と同様に数分かかる場合があります。

---

### ステップ 2 の別の方法（コマンドパレット）

`F1` キー（または `Ctrl+Shift+P`）を押してコマンドパレットを開き、`rebuild` と入力して **「Dev Containers: コンテナーのリビルド」** を選択します。

```
> rebuild
  Dev Containers: コンテナーのリビルド  ← これを選ぶ
```

---

## 次のステップ

環境構築が完了したら、`README_ja.md` を参照して pyMEA の使い方を確認してください。

Jupyter Notebook でのデータ解析も同じ Dev Container 環境でそのまま使えます。VS Code の拡張機能（Jupyter）がすでにインストールされているため、`.ipynb` ファイルを開くだけで使えます。
